import { API_BASILIUM } from "@/shared";

export const UPLOAD_PRODUCT = async () => {
  try {
    // const res = await API_BASILIUM.post("");
  } catch (err) {
    console.error(err);
    return false;
  }
};
